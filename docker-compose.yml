version: '3.7'

services:
  db:
    image: mongo:4.2
    container_name: park_app_db
    restart: always
    ports:
      - 5000:27017
    volumes:
      - db
      - $PWD/db/scripts:/docker-entrypoint-initdb.d
    env_file: $PWD/.env

volumes:
  db:
